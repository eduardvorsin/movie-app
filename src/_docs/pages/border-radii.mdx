import { Meta } from '@storybook/blocks';
import config from '../../../tailwind.config.ts';

<Meta title="Desing System/Border Radii" />

export const themeBorderRadii = config.theme.borderRadius;

export const borderRadii = [
	{ label:0.5, value: parseFloat(themeBorderRadii['0.5'])}, 
	{ label:1, value:parseFloat(themeBorderRadii['1']) },
	{ label:2, value:parseFloat(themeBorderRadii['2']) },
	{ label:3, value:parseFloat(themeBorderRadii['3']) },
	{ label:4, value:parseFloat(themeBorderRadii['4']) },
	{ label:5, value:parseFloat(themeBorderRadii['5']) },
	{ label:6, value:parseFloat(themeBorderRadii['6']) },
	{ label:'full', value:parseFloat(themeBorderRadii['full'])},
];

# Border Radii

<ul
	className={'!pl-0 !mt-4'}
>
	{borderRadii.map((radius) => (
		<li
			key={radius.label}
			className='!mt-0 !mb-4 !last:mb-0 flex gap-5 items-center'
		>
			<span className='basis-[100px]'>
				{radius.label}
			</span>
			<span className='basis-[250px]'>
				{`${(radius.value / 16).toFixed(3)}rem`} / {`${radius.value}px`}
			</span>
			<div
				className={'w-[72px] h-[72px] bg-blue-500'}
				style={{ borderRadius:`${radius.value}px` }}
			/>
		</li>
	))}
</ul>